# Generated from chatto/auth/v1/setup.proto. DO NOT EDIT.
# Generated by protoc-gen-connectrpc-py v0.12.1 with parameter "".
# ruff: noqa: PGH004
# ruff: noqa
# fmt: off

from __future__ import annotations

from typing import Protocol, TYPE_CHECKING

from connectrpc.client import ConnectClient, ConnectClientSync
from connectrpc.code import Code
from connectrpc.errors import ConnectError
from connectrpc.method import IdempotencyLevel, MethodInfo
from connectrpc.server import ConnectASGIApplication, ConnectWSGIApplication, DEFAULT_READ_MAX_BYTES, Endpoint, EndpointSync
from protobuf import DescService

from . import setup_pb
from .setup_pb import CompleteSetupRequest, CompleteSetupResponse

if TYPE_CHECKING:
    from collections.abc import AsyncGenerator, Iterable, Mapping

    from connectrpc.codec import Codec
    from connectrpc.compression import Compression
    from connectrpc.interceptor import Interceptor, InterceptorSync
    from connectrpc.request import Headers, RequestContext


class ServerSetupService(Protocol):
    """
    Creates the first owner on a new server. No session or setup secret is required.
    """
    async def complete_setup(self, request: CompleteSetupRequest, ctx: RequestContext[CompleteSetupRequest, CompleteSetupResponse], /) -> CompleteSetupResponse:
        """
        Public while setup is available. Atomically creates the owner and server
        settings, then permanently closes setup. Concurrent or later submissions
        fail with FAILED_PRECONDITION. A lost response can be recovered by signing in.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    @classmethod
    def desc(cls) -> DescService:
        """Returns the descriptor for this service."""
        return next(s for s in setup_pb.desc().services if s.type_name == 'chatto.auth.v1.ServerSetupService')

class ServerSetupServiceASGIApplication(ConnectASGIApplication[ServerSetupService]):
    def __init__(
        self,
        service: ServerSetupService | AsyncGenerator[ServerSetupService],
        *,
        interceptors: Iterable[Interceptor] = (),
        read_max_bytes: int | None = DEFAULT_READ_MAX_BYTES,
        compressions: Iterable[Compression] | None = None,
        codecs: Iterable[Codec] | None = None,
    ) -> None:
        super().__init__(
            service=service,
            endpoints=lambda svc: {
                "/chatto.auth.v1.ServerSetupService/CompleteSetup": Endpoint.unary(
                    method=MethodInfo(
                        name="CompleteSetup",
                        service_name="chatto.auth.v1.ServerSetupService",
                        input=CompleteSetupRequest,
                        output=CompleteSetupResponse,
                        idempotency_level=IdempotencyLevel.UNKNOWN,
                    ),
                    function=svc.complete_setup,
                ),
            },
            interceptors=interceptors,
            read_max_bytes=read_max_bytes,
            compressions=compressions,
            codecs=codecs,
        )

    @property
    def path(self) -> str:
        """Returns the URL path to mount the application to when serving multiple applications."""
        return "/chatto.auth.v1.ServerSetupService"


class ServerSetupServiceClient(ConnectClient):
    """
    Creates the first owner on a new server. No session or setup secret is required.
    """
    async def complete_setup(
        self,
        request: CompleteSetupRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> CompleteSetupResponse:
        """
        Public while setup is available. Atomically creates the owner and server
        settings, then permanently closes setup. Concurrent or later submissions
        fail with FAILED_PRECONDITION. A lost response can be recovered by signing in.
        """
        return await self.execute_unary(
            request=request,
            method=MethodInfo(
                name="CompleteSetup",
                service_name="chatto.auth.v1.ServerSetupService",
                input=CompleteSetupRequest,
                output=CompleteSetupResponse,
                idempotency_level=IdempotencyLevel.UNKNOWN,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )

class ServerSetupServiceSync(Protocol):
    """
    Creates the first owner on a new server. No session or setup secret is required.
    """
    def complete_setup(self, request: CompleteSetupRequest, ctx: RequestContext[CompleteSetupRequest, CompleteSetupResponse], /) -> CompleteSetupResponse:
        """
        Public while setup is available. Atomically creates the owner and server
        settings, then permanently closes setup. Concurrent or later submissions
        fail with FAILED_PRECONDITION. A lost response can be recovered by signing in.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    @classmethod
    def desc(cls) -> DescService:
        """Returns the descriptor for this service."""
        return next(s for s in setup_pb.desc().services if s.type_name == 'chatto.auth.v1.ServerSetupService')

class ServerSetupServiceWSGIApplication(ConnectWSGIApplication):
    def __init__(
        self,
        service: ServerSetupServiceSync,
        interceptors: Iterable[InterceptorSync] = (),
        read_max_bytes: int | None = DEFAULT_READ_MAX_BYTES,
        compressions: Iterable[Compression] | None = None,
        codecs: Iterable[Codec] | None = None,
    ) -> None:
        super().__init__(
            endpoints={
                "/chatto.auth.v1.ServerSetupService/CompleteSetup": EndpointSync.unary(
                    method=MethodInfo(
                        name="CompleteSetup",
                        service_name="chatto.auth.v1.ServerSetupService",
                        input=CompleteSetupRequest,
                        output=CompleteSetupResponse,
                        idempotency_level=IdempotencyLevel.UNKNOWN,
                    ),
                    function=service.complete_setup,
                ),
            },
            interceptors=interceptors,
            read_max_bytes=read_max_bytes,
            compressions=compressions,
            codecs=codecs,
        )

    @property
    def path(self) -> str:
        """Returns the URL path to mount the application to when serving multiple applications."""
        return "/chatto.auth.v1.ServerSetupService"


class ServerSetupServiceClientSync(ConnectClientSync):
    """
    Creates the first owner on a new server. No session or setup secret is required.
    """
    def complete_setup(
        self,
        request: CompleteSetupRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> CompleteSetupResponse:
        """
        Public while setup is available. Atomically creates the owner and server
        settings, then permanently closes setup. Concurrent or later submissions
        fail with FAILED_PRECONDITION. A lost response can be recovered by signing in.
        """
        return self.execute_unary(
            request=request,
            method=MethodInfo(
                name="CompleteSetup",
                service_name="chatto.auth.v1.ServerSetupService",
                input=CompleteSetupRequest,
                output=CompleteSetupResponse,
                idempotency_level=IdempotencyLevel.UNKNOWN,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )
