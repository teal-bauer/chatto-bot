# Generated from chatto/auth/v1/push_subscription_cleanup.proto. DO NOT EDIT.
# Generated by protoc-gen-connectrpc-py v0.12.1 with parameter "".
# ruff: noqa: PGH004
# ruff: noqa
# fmt: off

from __future__ import annotations

from typing import Protocol, TYPE_CHECKING

from connectrpc.client import ConnectClient, ConnectClientSync
from connectrpc.code import Code
from connectrpc.errors import ConnectError
from connectrpc.method import IdempotencyLevel, MethodInfo
from connectrpc.server import ConnectASGIApplication, ConnectWSGIApplication, DEFAULT_READ_MAX_BYTES, Endpoint, EndpointSync
from protobuf import DescService

from . import push_subscription_cleanup_pb
from .push_subscription_cleanup_pb import DeleteSubscriptionRequest, DeleteSubscriptionResponse

if TYPE_CHECKING:
    from collections.abc import AsyncGenerator, Iterable, Mapping

    from connectrpc.codec import Codec
    from connectrpc.compression import Compression
    from connectrpc.interceptor import Interceptor, InterceptorSync
    from connectrpc.request import Headers, RequestContext


class PushSubscriptionCleanupService(Protocol):
    """
    Public capability-authenticated cleanup for browser push subscriptions.
    """
    async def delete_subscription(self, request: DeleteSubscriptionRequest, ctx: RequestContext[DeleteSubscriptionRequest, DeleteSubscriptionResponse], /) -> DeleteSubscriptionResponse:
        """
        Removes the exact current subscription when its Push API auth secret and
        per-save cleanup token match. This public RPC requires those capabilities,
        not an authenticated account session, and does not reveal whether a match
        existed.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    @classmethod
    def desc(cls) -> DescService:
        """Returns the descriptor for this service."""
        return next(s for s in push_subscription_cleanup_pb.desc().services if s.type_name == 'chatto.auth.v1.PushSubscriptionCleanupService')

class PushSubscriptionCleanupServiceASGIApplication(ConnectASGIApplication[PushSubscriptionCleanupService]):
    def __init__(
        self,
        service: PushSubscriptionCleanupService | AsyncGenerator[PushSubscriptionCleanupService],
        *,
        interceptors: Iterable[Interceptor] = (),
        read_max_bytes: int | None = DEFAULT_READ_MAX_BYTES,
        compressions: Iterable[Compression] | None = None,
        codecs: Iterable[Codec] | None = None,
    ) -> None:
        super().__init__(
            service=service,
            endpoints=lambda svc: {
                "/chatto.auth.v1.PushSubscriptionCleanupService/DeleteSubscription": Endpoint.unary(
                    method=MethodInfo(
                        name="DeleteSubscription",
                        service_name="chatto.auth.v1.PushSubscriptionCleanupService",
                        input=DeleteSubscriptionRequest,
                        output=DeleteSubscriptionResponse,
                        idempotency_level=IdempotencyLevel.IDEMPOTENT,
                    ),
                    function=svc.delete_subscription,
                ),
            },
            interceptors=interceptors,
            read_max_bytes=read_max_bytes,
            compressions=compressions,
            codecs=codecs,
        )

    @property
    def path(self) -> str:
        """Returns the URL path to mount the application to when serving multiple applications."""
        return "/chatto.auth.v1.PushSubscriptionCleanupService"


class PushSubscriptionCleanupServiceClient(ConnectClient):
    """
    Public capability-authenticated cleanup for browser push subscriptions.
    """
    async def delete_subscription(
        self,
        request: DeleteSubscriptionRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> DeleteSubscriptionResponse:
        """
        Removes the exact current subscription when its Push API auth secret and
        per-save cleanup token match. This public RPC requires those capabilities,
        not an authenticated account session, and does not reveal whether a match
        existed.
        """
        return await self.execute_unary(
            request=request,
            method=MethodInfo(
                name="DeleteSubscription",
                service_name="chatto.auth.v1.PushSubscriptionCleanupService",
                input=DeleteSubscriptionRequest,
                output=DeleteSubscriptionResponse,
                idempotency_level=IdempotencyLevel.IDEMPOTENT,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )

class PushSubscriptionCleanupServiceSync(Protocol):
    """
    Public capability-authenticated cleanup for browser push subscriptions.
    """
    def delete_subscription(self, request: DeleteSubscriptionRequest, ctx: RequestContext[DeleteSubscriptionRequest, DeleteSubscriptionResponse], /) -> DeleteSubscriptionResponse:
        """
        Removes the exact current subscription when its Push API auth secret and
        per-save cleanup token match. This public RPC requires those capabilities,
        not an authenticated account session, and does not reveal whether a match
        existed.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    @classmethod
    def desc(cls) -> DescService:
        """Returns the descriptor for this service."""
        return next(s for s in push_subscription_cleanup_pb.desc().services if s.type_name == 'chatto.auth.v1.PushSubscriptionCleanupService')

class PushSubscriptionCleanupServiceWSGIApplication(ConnectWSGIApplication):
    def __init__(
        self,
        service: PushSubscriptionCleanupServiceSync,
        interceptors: Iterable[InterceptorSync] = (),
        read_max_bytes: int | None = DEFAULT_READ_MAX_BYTES,
        compressions: Iterable[Compression] | None = None,
        codecs: Iterable[Codec] | None = None,
    ) -> None:
        super().__init__(
            endpoints={
                "/chatto.auth.v1.PushSubscriptionCleanupService/DeleteSubscription": EndpointSync.unary(
                    method=MethodInfo(
                        name="DeleteSubscription",
                        service_name="chatto.auth.v1.PushSubscriptionCleanupService",
                        input=DeleteSubscriptionRequest,
                        output=DeleteSubscriptionResponse,
                        idempotency_level=IdempotencyLevel.IDEMPOTENT,
                    ),
                    function=service.delete_subscription,
                ),
            },
            interceptors=interceptors,
            read_max_bytes=read_max_bytes,
            compressions=compressions,
            codecs=codecs,
        )

    @property
    def path(self) -> str:
        """Returns the URL path to mount the application to when serving multiple applications."""
        return "/chatto.auth.v1.PushSubscriptionCleanupService"


class PushSubscriptionCleanupServiceClientSync(ConnectClientSync):
    """
    Public capability-authenticated cleanup for browser push subscriptions.
    """
    def delete_subscription(
        self,
        request: DeleteSubscriptionRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> DeleteSubscriptionResponse:
        """
        Removes the exact current subscription when its Push API auth secret and
        per-save cleanup token match. This public RPC requires those capabilities,
        not an authenticated account session, and does not reveal whether a match
        existed.
        """
        return self.execute_unary(
            request=request,
            method=MethodInfo(
                name="DeleteSubscription",
                service_name="chatto.auth.v1.PushSubscriptionCleanupService",
                input=DeleteSubscriptionRequest,
                output=DeleteSubscriptionResponse,
                idempotency_level=IdempotencyLevel.IDEMPOTENT,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )
