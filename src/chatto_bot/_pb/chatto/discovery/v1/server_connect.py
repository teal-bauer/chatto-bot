# Generated from chatto/discovery/v1/server.proto. DO NOT EDIT.
# Generated by protoc-gen-connectrpc-py v0.12.1 with parameter "".
# ruff: noqa: PGH004
# ruff: noqa
# fmt: off

from __future__ import annotations

from typing import Protocol, TYPE_CHECKING

from connectrpc.client import ConnectClient, ConnectClientSync
from connectrpc.code import Code
from connectrpc.errors import ConnectError
from connectrpc.method import IdempotencyLevel, MethodInfo
from connectrpc.server import ConnectASGIApplication, ConnectWSGIApplication, DEFAULT_READ_MAX_BYTES, Endpoint, EndpointSync
from protobuf import DescService

from . import server_pb
from .server_pb import GetServerRequest, GetServerResponse, ListNeighborsRequest, ListNeighborsResponse

if TYPE_CHECKING:
    from collections.abc import AsyncGenerator, Iterable, Mapping

    from connectrpc.codec import Codec
    from connectrpc.compression import Compression
    from connectrpc.interceptor import Interceptor, InterceptorSync
    from connectrpc.request import Headers, RequestContext


class ServerDiscoveryService(Protocol):
    """
    Provides unauthenticated server discovery metadata.
    """
    async def get_server(self, request: GetServerRequest, ctx: RequestContext[GetServerRequest, GetServerResponse], /) -> GetServerResponse:
        """
        Returns public server metadata, branding, registration status, and login
        provider information. This fully public RPC is available before login so
        clients can render the first server screen and authentication choices.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    async def list_neighbors(self, request: ListNeighborsRequest, ctx: RequestContext[ListNeighborsRequest, ListNeighborsResponse], /) -> ListNeighborsResponse:
        """
        Returns the public Neighbor directory without requiring a session.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    @classmethod
    def desc(cls) -> DescService:
        """Returns the descriptor for this service."""
        return next(s for s in server_pb.desc().services if s.type_name == 'chatto.discovery.v1.ServerDiscoveryService')

class ServerDiscoveryServiceASGIApplication(ConnectASGIApplication[ServerDiscoveryService]):
    def __init__(
        self,
        service: ServerDiscoveryService | AsyncGenerator[ServerDiscoveryService],
        *,
        interceptors: Iterable[Interceptor] = (),
        read_max_bytes: int | None = DEFAULT_READ_MAX_BYTES,
        compressions: Iterable[Compression] | None = None,
        codecs: Iterable[Codec] | None = None,
    ) -> None:
        super().__init__(
            service=service,
            endpoints=lambda svc: {
                "/chatto.discovery.v1.ServerDiscoveryService/GetServer": Endpoint.unary(
                    method=MethodInfo(
                        name="GetServer",
                        service_name="chatto.discovery.v1.ServerDiscoveryService",
                        input=GetServerRequest,
                        output=GetServerResponse,
                        idempotency_level=IdempotencyLevel.NO_SIDE_EFFECTS,
                    ),
                    function=svc.get_server,
                ),
                "/chatto.discovery.v1.ServerDiscoveryService/ListNeighbors": Endpoint.unary(
                    method=MethodInfo(
                        name="ListNeighbors",
                        service_name="chatto.discovery.v1.ServerDiscoveryService",
                        input=ListNeighborsRequest,
                        output=ListNeighborsResponse,
                        idempotency_level=IdempotencyLevel.NO_SIDE_EFFECTS,
                    ),
                    function=svc.list_neighbors,
                ),
            },
            interceptors=interceptors,
            read_max_bytes=read_max_bytes,
            compressions=compressions,
            codecs=codecs,
        )

    @property
    def path(self) -> str:
        """Returns the URL path to mount the application to when serving multiple applications."""
        return "/chatto.discovery.v1.ServerDiscoveryService"


class ServerDiscoveryServiceClient(ConnectClient):
    """
    Provides unauthenticated server discovery metadata.
    """
    async def get_server(
        self,
        request: GetServerRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
        use_get: bool = False,
    ) -> GetServerResponse:
        """
        Returns public server metadata, branding, registration status, and login
        provider information. This fully public RPC is available before login so
        clients can render the first server screen and authentication choices.
        """
        return await self.execute_unary(
            request=request,
            method=MethodInfo(
                name="GetServer",
                service_name="chatto.discovery.v1.ServerDiscoveryService",
                input=GetServerRequest,
                output=GetServerResponse,
                idempotency_level=IdempotencyLevel.NO_SIDE_EFFECTS,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
            use_get=use_get,
        )

    async def list_neighbors(
        self,
        request: ListNeighborsRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
        use_get: bool = False,
    ) -> ListNeighborsResponse:
        """
        Returns the public Neighbor directory without requiring a session.
        """
        return await self.execute_unary(
            request=request,
            method=MethodInfo(
                name="ListNeighbors",
                service_name="chatto.discovery.v1.ServerDiscoveryService",
                input=ListNeighborsRequest,
                output=ListNeighborsResponse,
                idempotency_level=IdempotencyLevel.NO_SIDE_EFFECTS,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
            use_get=use_get,
        )

class ServerDiscoveryServiceSync(Protocol):
    """
    Provides unauthenticated server discovery metadata.
    """
    def get_server(self, request: GetServerRequest, ctx: RequestContext[GetServerRequest, GetServerResponse], /) -> GetServerResponse:
        """
        Returns public server metadata, branding, registration status, and login
        provider information. This fully public RPC is available before login so
        clients can render the first server screen and authentication choices.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    def list_neighbors(self, request: ListNeighborsRequest, ctx: RequestContext[ListNeighborsRequest, ListNeighborsResponse], /) -> ListNeighborsResponse:
        """
        Returns the public Neighbor directory without requiring a session.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    @classmethod
    def desc(cls) -> DescService:
        """Returns the descriptor for this service."""
        return next(s for s in server_pb.desc().services if s.type_name == 'chatto.discovery.v1.ServerDiscoveryService')

class ServerDiscoveryServiceWSGIApplication(ConnectWSGIApplication):
    def __init__(
        self,
        service: ServerDiscoveryServiceSync,
        interceptors: Iterable[InterceptorSync] = (),
        read_max_bytes: int | None = DEFAULT_READ_MAX_BYTES,
        compressions: Iterable[Compression] | None = None,
        codecs: Iterable[Codec] | None = None,
    ) -> None:
        super().__init__(
            endpoints={
                "/chatto.discovery.v1.ServerDiscoveryService/GetServer": EndpointSync.unary(
                    method=MethodInfo(
                        name="GetServer",
                        service_name="chatto.discovery.v1.ServerDiscoveryService",
                        input=GetServerRequest,
                        output=GetServerResponse,
                        idempotency_level=IdempotencyLevel.NO_SIDE_EFFECTS,
                    ),
                    function=service.get_server,
                ),
                "/chatto.discovery.v1.ServerDiscoveryService/ListNeighbors": EndpointSync.unary(
                    method=MethodInfo(
                        name="ListNeighbors",
                        service_name="chatto.discovery.v1.ServerDiscoveryService",
                        input=ListNeighborsRequest,
                        output=ListNeighborsResponse,
                        idempotency_level=IdempotencyLevel.NO_SIDE_EFFECTS,
                    ),
                    function=service.list_neighbors,
                ),
            },
            interceptors=interceptors,
            read_max_bytes=read_max_bytes,
            compressions=compressions,
            codecs=codecs,
        )

    @property
    def path(self) -> str:
        """Returns the URL path to mount the application to when serving multiple applications."""
        return "/chatto.discovery.v1.ServerDiscoveryService"


class ServerDiscoveryServiceClientSync(ConnectClientSync):
    """
    Provides unauthenticated server discovery metadata.
    """
    def get_server(
        self,
        request: GetServerRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
        use_get: bool = False,
    ) -> GetServerResponse:
        """
        Returns public server metadata, branding, registration status, and login
        provider information. This fully public RPC is available before login so
        clients can render the first server screen and authentication choices.
        """
        return self.execute_unary(
            request=request,
            method=MethodInfo(
                name="GetServer",
                service_name="chatto.discovery.v1.ServerDiscoveryService",
                input=GetServerRequest,
                output=GetServerResponse,
                idempotency_level=IdempotencyLevel.NO_SIDE_EFFECTS,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
            use_get=use_get,
        )
    def list_neighbors(
        self,
        request: ListNeighborsRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
        use_get: bool = False,
    ) -> ListNeighborsResponse:
        """
        Returns the public Neighbor directory without requiring a session.
        """
        return self.execute_unary(
            request=request,
            method=MethodInfo(
                name="ListNeighbors",
                service_name="chatto.discovery.v1.ServerDiscoveryService",
                input=ListNeighborsRequest,
                output=ListNeighborsResponse,
                idempotency_level=IdempotencyLevel.NO_SIDE_EFFECTS,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
            use_get=use_get,
        )
