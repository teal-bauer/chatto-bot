# Generated from chatto/api/v1/permissions.proto. DO NOT EDIT.
# Generated by protoc-gen-connectrpc-py v0.12.1 with parameter "".
# ruff: noqa: PGH004
# ruff: noqa
# fmt: off

from __future__ import annotations

from typing import Protocol, TYPE_CHECKING

from connectrpc.client import ConnectClient, ConnectClientSync
from connectrpc.code import Code
from connectrpc.errors import ConnectError
from connectrpc.method import IdempotencyLevel, MethodInfo
from connectrpc.server import ConnectASGIApplication, ConnectWSGIApplication, DEFAULT_READ_MAX_BYTES, Endpoint, EndpointSync
from protobuf import DescService

from . import permissions_pb
from .permissions_pb import ListEffectivePermissionsRequest, ListEffectivePermissionsResponse

if TYPE_CHECKING:
    from collections.abc import AsyncGenerator, Iterable, Mapping

    from connectrpc.codec import Codec
    from connectrpc.compression import Compression
    from connectrpc.interceptor import Interceptor, InterceptorSync
    from connectrpc.request import Headers, RequestContext


class PermissionService(Protocol):
    """
    Read-only effective authority. Configuration and explanations remain in
    chatto.admin.v1.AdminPermissionService.
    """
    async def list_effective_permissions(self, request: ListEffectivePermissionsRequest, ctx: RequestContext[ListEffectivePermissionsRequest, ListEffectivePermissionsResponse], /) -> ListEffectivePermissionsResponse:
        """
        Any authenticated member may read a bot's effective permissions. Human
        targets require user.manage-permissions, including self-targeted reads.
        Bot authority includes the owner's current limits. Hidden room identities,
        stored grants, credentials, and individual DM participants are not exposed.
        Missing or deleted targets return NOT_FOUND. Each call returns the complete
        visible set from one coherent read, without pagination or truncation. Clients
        should refresh visible views (the bundled client uses 30 seconds) and clear
        the view on close or read failure.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    @classmethod
    def desc(cls) -> DescService:
        """Returns the descriptor for this service."""
        return next(s for s in permissions_pb.desc().services if s.type_name == 'chatto.api.v1.PermissionService')

class PermissionServiceASGIApplication(ConnectASGIApplication[PermissionService]):
    def __init__(
        self,
        service: PermissionService | AsyncGenerator[PermissionService],
        *,
        interceptors: Iterable[Interceptor] = (),
        read_max_bytes: int | None = DEFAULT_READ_MAX_BYTES,
        compressions: Iterable[Compression] | None = None,
        codecs: Iterable[Codec] | None = None,
    ) -> None:
        super().__init__(
            service=service,
            endpoints=lambda svc: {
                "/chatto.api.v1.PermissionService/ListEffectivePermissions": Endpoint.unary(
                    method=MethodInfo(
                        name="ListEffectivePermissions",
                        service_name="chatto.api.v1.PermissionService",
                        input=ListEffectivePermissionsRequest,
                        output=ListEffectivePermissionsResponse,
                        idempotency_level=IdempotencyLevel.UNKNOWN,
                    ),
                    function=svc.list_effective_permissions,
                ),
            },
            interceptors=interceptors,
            read_max_bytes=read_max_bytes,
            compressions=compressions,
            codecs=codecs,
        )

    @property
    def path(self) -> str:
        """Returns the URL path to mount the application to when serving multiple applications."""
        return "/chatto.api.v1.PermissionService"


class PermissionServiceClient(ConnectClient):
    """
    Read-only effective authority. Configuration and explanations remain in
    chatto.admin.v1.AdminPermissionService.
    """
    async def list_effective_permissions(
        self,
        request: ListEffectivePermissionsRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> ListEffectivePermissionsResponse:
        """
        Any authenticated member may read a bot's effective permissions. Human
        targets require user.manage-permissions, including self-targeted reads.
        Bot authority includes the owner's current limits. Hidden room identities,
        stored grants, credentials, and individual DM participants are not exposed.
        Missing or deleted targets return NOT_FOUND. Each call returns the complete
        visible set from one coherent read, without pagination or truncation. Clients
        should refresh visible views (the bundled client uses 30 seconds) and clear
        the view on close or read failure.
        """
        return await self.execute_unary(
            request=request,
            method=MethodInfo(
                name="ListEffectivePermissions",
                service_name="chatto.api.v1.PermissionService",
                input=ListEffectivePermissionsRequest,
                output=ListEffectivePermissionsResponse,
                idempotency_level=IdempotencyLevel.UNKNOWN,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )

class PermissionServiceSync(Protocol):
    """
    Read-only effective authority. Configuration and explanations remain in
    chatto.admin.v1.AdminPermissionService.
    """
    def list_effective_permissions(self, request: ListEffectivePermissionsRequest, ctx: RequestContext[ListEffectivePermissionsRequest, ListEffectivePermissionsResponse], /) -> ListEffectivePermissionsResponse:
        """
        Any authenticated member may read a bot's effective permissions. Human
        targets require user.manage-permissions, including self-targeted reads.
        Bot authority includes the owner's current limits. Hidden room identities,
        stored grants, credentials, and individual DM participants are not exposed.
        Missing or deleted targets return NOT_FOUND. Each call returns the complete
        visible set from one coherent read, without pagination or truncation. Clients
        should refresh visible views (the bundled client uses 30 seconds) and clear
        the view on close or read failure.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    @classmethod
    def desc(cls) -> DescService:
        """Returns the descriptor for this service."""
        return next(s for s in permissions_pb.desc().services if s.type_name == 'chatto.api.v1.PermissionService')

class PermissionServiceWSGIApplication(ConnectWSGIApplication):
    def __init__(
        self,
        service: PermissionServiceSync,
        interceptors: Iterable[InterceptorSync] = (),
        read_max_bytes: int | None = DEFAULT_READ_MAX_BYTES,
        compressions: Iterable[Compression] | None = None,
        codecs: Iterable[Codec] | None = None,
    ) -> None:
        super().__init__(
            endpoints={
                "/chatto.api.v1.PermissionService/ListEffectivePermissions": EndpointSync.unary(
                    method=MethodInfo(
                        name="ListEffectivePermissions",
                        service_name="chatto.api.v1.PermissionService",
                        input=ListEffectivePermissionsRequest,
                        output=ListEffectivePermissionsResponse,
                        idempotency_level=IdempotencyLevel.UNKNOWN,
                    ),
                    function=service.list_effective_permissions,
                ),
            },
            interceptors=interceptors,
            read_max_bytes=read_max_bytes,
            compressions=compressions,
            codecs=codecs,
        )

    @property
    def path(self) -> str:
        """Returns the URL path to mount the application to when serving multiple applications."""
        return "/chatto.api.v1.PermissionService"


class PermissionServiceClientSync(ConnectClientSync):
    """
    Read-only effective authority. Configuration and explanations remain in
    chatto.admin.v1.AdminPermissionService.
    """
    def list_effective_permissions(
        self,
        request: ListEffectivePermissionsRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> ListEffectivePermissionsResponse:
        """
        Any authenticated member may read a bot's effective permissions. Human
        targets require user.manage-permissions, including self-targeted reads.
        Bot authority includes the owner's current limits. Hidden room identities,
        stored grants, credentials, and individual DM participants are not exposed.
        Missing or deleted targets return NOT_FOUND. Each call returns the complete
        visible set from one coherent read, without pagination or truncation. Clients
        should refresh visible views (the bundled client uses 30 seconds) and clear
        the view on close or read failure.
        """
        return self.execute_unary(
            request=request,
            method=MethodInfo(
                name="ListEffectivePermissions",
                service_name="chatto.api.v1.PermissionService",
                input=ListEffectivePermissionsRequest,
                output=ListEffectivePermissionsResponse,
                idempotency_level=IdempotencyLevel.UNKNOWN,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )
