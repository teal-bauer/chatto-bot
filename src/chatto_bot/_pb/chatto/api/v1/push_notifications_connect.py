# Generated from chatto/api/v1/push_notifications.proto. DO NOT EDIT.
# Generated by protoc-gen-connectrpc-py v0.12.1 with parameter "".
# ruff: noqa: PGH004
# ruff: noqa
# fmt: off

from __future__ import annotations

from typing import Protocol, TYPE_CHECKING

from connectrpc.client import ConnectClient, ConnectClientSync
from connectrpc.code import Code
from connectrpc.errors import ConnectError
from connectrpc.method import IdempotencyLevel, MethodInfo
from connectrpc.server import ConnectASGIApplication, ConnectWSGIApplication, DEFAULT_READ_MAX_BYTES, Endpoint, EndpointSync
from protobuf import DescService

from . import push_notifications_pb
from .push_notifications_pb import SendTestNotificationRequest, SendTestNotificationResponse, SubscribeRequest, SubscribeResponse, UnsubscribeRequest, UnsubscribeResponse

if TYPE_CHECKING:
    from collections.abc import AsyncGenerator, Iterable, Mapping

    from connectrpc.codec import Codec
    from connectrpc.compression import Compression
    from connectrpc.interceptor import Interceptor, InterceptorSync
    from connectrpc.request import Headers, RequestContext


class PushNotificationService(Protocol):
    """
    Browser Web Push subscription commands for the authenticated user.
    """
    async def subscribe(self, request: SubscribeRequest, ctx: RequestContext[SubscribeRequest, SubscribeResponse], /) -> SubscribeResponse:
        """
        Stores or updates the caller's browser push subscription.

        The server must have Web Push configured. Clients normally call this after
        the browser grants notification permission and returns a PushSubscription.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    async def unsubscribe(self, request: UnsubscribeRequest, ctx: RequestContext[UnsubscribeRequest, UnsubscribeResponse], /) -> UnsubscribeResponse:
        """
        Removes the caller's browser push subscription by endpoint.

        The call is idempotent: removing an unknown endpoint still succeeds.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    async def send_test_notification(self, request: SendTestNotificationRequest, ctx: RequestContext[SendTestNotificationRequest, SendTestNotificationResponse], /) -> SendTestNotificationResponse:
        """
        Sends a test notification to the caller's registered browser subscriptions.
        Calls are rate-limited per account. Delivery failures return a generic
        unavailable error without exposing the push provider's response body.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    @classmethod
    def desc(cls) -> DescService:
        """Returns the descriptor for this service."""
        return next(s for s in push_notifications_pb.desc().services if s.type_name == 'chatto.api.v1.PushNotificationService')

class PushNotificationServiceASGIApplication(ConnectASGIApplication[PushNotificationService]):
    def __init__(
        self,
        service: PushNotificationService | AsyncGenerator[PushNotificationService],
        *,
        interceptors: Iterable[Interceptor] = (),
        read_max_bytes: int | None = DEFAULT_READ_MAX_BYTES,
        compressions: Iterable[Compression] | None = None,
        codecs: Iterable[Codec] | None = None,
    ) -> None:
        super().__init__(
            service=service,
            endpoints=lambda svc: {
                "/chatto.api.v1.PushNotificationService/Subscribe": Endpoint.unary(
                    method=MethodInfo(
                        name="Subscribe",
                        service_name="chatto.api.v1.PushNotificationService",
                        input=SubscribeRequest,
                        output=SubscribeResponse,
                        idempotency_level=IdempotencyLevel.UNKNOWN,
                    ),
                    function=svc.subscribe,
                ),
                "/chatto.api.v1.PushNotificationService/Unsubscribe": Endpoint.unary(
                    method=MethodInfo(
                        name="Unsubscribe",
                        service_name="chatto.api.v1.PushNotificationService",
                        input=UnsubscribeRequest,
                        output=UnsubscribeResponse,
                        idempotency_level=IdempotencyLevel.IDEMPOTENT,
                    ),
                    function=svc.unsubscribe,
                ),
                "/chatto.api.v1.PushNotificationService/SendTestNotification": Endpoint.unary(
                    method=MethodInfo(
                        name="SendTestNotification",
                        service_name="chatto.api.v1.PushNotificationService",
                        input=SendTestNotificationRequest,
                        output=SendTestNotificationResponse,
                        idempotency_level=IdempotencyLevel.UNKNOWN,
                    ),
                    function=svc.send_test_notification,
                ),
            },
            interceptors=interceptors,
            read_max_bytes=read_max_bytes,
            compressions=compressions,
            codecs=codecs,
        )

    @property
    def path(self) -> str:
        """Returns the URL path to mount the application to when serving multiple applications."""
        return "/chatto.api.v1.PushNotificationService"


class PushNotificationServiceClient(ConnectClient):
    """
    Browser Web Push subscription commands for the authenticated user.
    """
    async def subscribe(
        self,
        request: SubscribeRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> SubscribeResponse:
        """
        Stores or updates the caller's browser push subscription.

        The server must have Web Push configured. Clients normally call this after
        the browser grants notification permission and returns a PushSubscription.
        """
        return await self.execute_unary(
            request=request,
            method=MethodInfo(
                name="Subscribe",
                service_name="chatto.api.v1.PushNotificationService",
                input=SubscribeRequest,
                output=SubscribeResponse,
                idempotency_level=IdempotencyLevel.UNKNOWN,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )

    async def unsubscribe(
        self,
        request: UnsubscribeRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> UnsubscribeResponse:
        """
        Removes the caller's browser push subscription by endpoint.

        The call is idempotent: removing an unknown endpoint still succeeds.
        """
        return await self.execute_unary(
            request=request,
            method=MethodInfo(
                name="Unsubscribe",
                service_name="chatto.api.v1.PushNotificationService",
                input=UnsubscribeRequest,
                output=UnsubscribeResponse,
                idempotency_level=IdempotencyLevel.IDEMPOTENT,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )

    async def send_test_notification(
        self,
        request: SendTestNotificationRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> SendTestNotificationResponse:
        """
        Sends a test notification to the caller's registered browser subscriptions.
        Calls are rate-limited per account. Delivery failures return a generic
        unavailable error without exposing the push provider's response body.
        """
        return await self.execute_unary(
            request=request,
            method=MethodInfo(
                name="SendTestNotification",
                service_name="chatto.api.v1.PushNotificationService",
                input=SendTestNotificationRequest,
                output=SendTestNotificationResponse,
                idempotency_level=IdempotencyLevel.UNKNOWN,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )

class PushNotificationServiceSync(Protocol):
    """
    Browser Web Push subscription commands for the authenticated user.
    """
    def subscribe(self, request: SubscribeRequest, ctx: RequestContext[SubscribeRequest, SubscribeResponse], /) -> SubscribeResponse:
        """
        Stores or updates the caller's browser push subscription.

        The server must have Web Push configured. Clients normally call this after
        the browser grants notification permission and returns a PushSubscription.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    def unsubscribe(self, request: UnsubscribeRequest, ctx: RequestContext[UnsubscribeRequest, UnsubscribeResponse], /) -> UnsubscribeResponse:
        """
        Removes the caller's browser push subscription by endpoint.

        The call is idempotent: removing an unknown endpoint still succeeds.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    def send_test_notification(self, request: SendTestNotificationRequest, ctx: RequestContext[SendTestNotificationRequest, SendTestNotificationResponse], /) -> SendTestNotificationResponse:
        """
        Sends a test notification to the caller's registered browser subscriptions.
        Calls are rate-limited per account. Delivery failures return a generic
        unavailable error without exposing the push provider's response body.
        """
        raise ConnectError(Code.UNIMPLEMENTED, 'Not implemented')

    @classmethod
    def desc(cls) -> DescService:
        """Returns the descriptor for this service."""
        return next(s for s in push_notifications_pb.desc().services if s.type_name == 'chatto.api.v1.PushNotificationService')

class PushNotificationServiceWSGIApplication(ConnectWSGIApplication):
    def __init__(
        self,
        service: PushNotificationServiceSync,
        interceptors: Iterable[InterceptorSync] = (),
        read_max_bytes: int | None = DEFAULT_READ_MAX_BYTES,
        compressions: Iterable[Compression] | None = None,
        codecs: Iterable[Codec] | None = None,
    ) -> None:
        super().__init__(
            endpoints={
                "/chatto.api.v1.PushNotificationService/Subscribe": EndpointSync.unary(
                    method=MethodInfo(
                        name="Subscribe",
                        service_name="chatto.api.v1.PushNotificationService",
                        input=SubscribeRequest,
                        output=SubscribeResponse,
                        idempotency_level=IdempotencyLevel.UNKNOWN,
                    ),
                    function=service.subscribe,
                ),
                "/chatto.api.v1.PushNotificationService/Unsubscribe": EndpointSync.unary(
                    method=MethodInfo(
                        name="Unsubscribe",
                        service_name="chatto.api.v1.PushNotificationService",
                        input=UnsubscribeRequest,
                        output=UnsubscribeResponse,
                        idempotency_level=IdempotencyLevel.IDEMPOTENT,
                    ),
                    function=service.unsubscribe,
                ),
                "/chatto.api.v1.PushNotificationService/SendTestNotification": EndpointSync.unary(
                    method=MethodInfo(
                        name="SendTestNotification",
                        service_name="chatto.api.v1.PushNotificationService",
                        input=SendTestNotificationRequest,
                        output=SendTestNotificationResponse,
                        idempotency_level=IdempotencyLevel.UNKNOWN,
                    ),
                    function=service.send_test_notification,
                ),
            },
            interceptors=interceptors,
            read_max_bytes=read_max_bytes,
            compressions=compressions,
            codecs=codecs,
        )

    @property
    def path(self) -> str:
        """Returns the URL path to mount the application to when serving multiple applications."""
        return "/chatto.api.v1.PushNotificationService"


class PushNotificationServiceClientSync(ConnectClientSync):
    """
    Browser Web Push subscription commands for the authenticated user.
    """
    def subscribe(
        self,
        request: SubscribeRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> SubscribeResponse:
        """
        Stores or updates the caller's browser push subscription.

        The server must have Web Push configured. Clients normally call this after
        the browser grants notification permission and returns a PushSubscription.
        """
        return self.execute_unary(
            request=request,
            method=MethodInfo(
                name="Subscribe",
                service_name="chatto.api.v1.PushNotificationService",
                input=SubscribeRequest,
                output=SubscribeResponse,
                idempotency_level=IdempotencyLevel.UNKNOWN,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )
    def unsubscribe(
        self,
        request: UnsubscribeRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> UnsubscribeResponse:
        """
        Removes the caller's browser push subscription by endpoint.

        The call is idempotent: removing an unknown endpoint still succeeds.
        """
        return self.execute_unary(
            request=request,
            method=MethodInfo(
                name="Unsubscribe",
                service_name="chatto.api.v1.PushNotificationService",
                input=UnsubscribeRequest,
                output=UnsubscribeResponse,
                idempotency_level=IdempotencyLevel.IDEMPOTENT,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )
    def send_test_notification(
        self,
        request: SendTestNotificationRequest,
        *,
        headers: Headers | Mapping[str, str] | None = None, 
        timeout_ms: int | None = None,
    ) -> SendTestNotificationResponse:
        """
        Sends a test notification to the caller's registered browser subscriptions.
        Calls are rate-limited per account. Delivery failures return a generic
        unavailable error without exposing the push provider's response body.
        """
        return self.execute_unary(
            request=request,
            method=MethodInfo(
                name="SendTestNotification",
                service_name="chatto.api.v1.PushNotificationService",
                input=SendTestNotificationRequest,
                output=SendTestNotificationResponse,
                idempotency_level=IdempotencyLevel.UNKNOWN,
            ),
            headers=headers,
            timeout_ms=timeout_ms,
        )
