# Chatto Bot configuration
# Secrets (email, password, session) should stay in .env or environment variables.

instance: https://chat.chatto.run
prefix: "!"
dms: true

spaces:
  - S6dEs8FO8eFrxFA  # Chatto Community

admins:
  - teal
  - hmans

extensions:
  - plugins.admin
  - plugins.remind
